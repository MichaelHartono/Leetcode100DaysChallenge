func removeElement(nums []int, val int) int {
    // we use j to store the position to be swapped
    // the idea is if curr = val, swap nums[j] with curr

    j := 0
    for _, curr := range nums {
        if (curr != val){
            nums[j] = curr
            j++
        }
    }

    return j
}
